@{
}

@model Book

<div class="card card-outline-primary m-2 p-1 shadow-lg">
    <div class="bg-light p-1 card-header bg-dark">
        <h4 class="text-light">
            @Model.Title
            <span class="badge bg-info" style="float:right">
                <small>$@Model.Price</small>
            </span>
        </h4>
    </div>
    <div class="card-text p-1">@Model.Category @Model.Classification</div>
    <div class="card-text p-1">Author: @Model.Author</div>
    <div class="card-text p-1">Publisher: @Model.Publisher</div>
    <div class="card-text p-1">ISBN: @Model.Isbn</div>
    <div class="card-text p-1">Page Count: @Model.PageCount</div>

    <form method="post" asp-page="/Cart">
        <input type="hidden" asp-for="@Model.BookId"/>
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()"/>

        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-end">Add</button>
        </span>
    </form>
</div>

